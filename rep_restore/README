#PG Restore COINS DB
These scripts will restore a database named `coins` on the current host from the latest backup of production.
Place the driver script in /etc/cron.daily to run the restore daily.
